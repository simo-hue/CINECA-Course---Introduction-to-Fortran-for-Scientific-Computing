3. Scrivere una subroutine (o funzione) che calcoli il prodotto matriciale fra due matrici quadrate reali `N x N`.

   Utilizzando la traccia dentro `matmul_showdown.f90`, confrontare il tempo di esecuzione con la funzioni intrinseche di Fortran `MATMUL` per diverse taglie `N` e per diversi livelli di ottimizzazione del compilatore, ossia
   ```bash
   gfortran -O0 ...
   gfortran -O1 ...
   gfortran -O2 ...
   gfortran -O3 ...
   ```
 

