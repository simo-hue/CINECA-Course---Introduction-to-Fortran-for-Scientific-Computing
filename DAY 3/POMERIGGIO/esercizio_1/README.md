
   _Opzionale_: dato il file `db.dat`, che contiene una lista di informazioni anagrafiche con formato
   ```
   Nome1 Cognome1 DataNascita1 LuogoNascita1
   Nome2 Cognome2 DataNascita2 LuogoNascita2
   ...
   ```
   scrivere un programma che legga il file e lo salvi in un array di tipi derivati di tipo `person_t`. 
   Stampare il database così letto con la procedura `show`.


2. Espandere l'esempio del modulo `vector_type`, implementando:
   - La somma e la sottrazione fra tipi derivati `vector_t`.
   - La somma e la sottrazione fra un tipo `vector_t` e uno scalare `REAL`.
   Testare l'implementazione col programma `test_vectors.f90`
